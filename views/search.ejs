<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="bg-gray-100 font-sans">
    <div class="max-w-md mx-auto bg-white shadow-lg rounded-lg overflow-hidden md:max-w-lg">
        <!-- Header -->
        <div class="bg-green-700 p-4 flex items-center">
            <a href="/chat-list" class="text-white mr-4"><i class="fas fa-arrow-left text-xl"></i></a>
            <h1 class="text-white text-2xl font-semibold">Search</h1>
        </div>

        <!-- Search Input -->
        <div class="p-4 border-b border-gray-200">
            <div class="relative">
                <input type="text" placeholder="Search chats, messages, users..." class="w-full p-3 pl-10 rounded-lg bg-gray-100 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-green-500">
                <div class="absolute inset-y-0 left-0 flex items-center pl-3">
                    <i class="fas fa-search text-gray-400"></i>
                </div>
            </div>
        </div>

        <!-- Search Filters (Placeholder) -->
        <div class="p-4 border-b border-gray-200">
            <h3 class="text-lg font-semibold text-gray-800 mb-3">Filters</h3>
            <div class="flex flex-wrap gap-2">
                <span class="bg-green-100 text-green-800 text-sm px-3 py-1 rounded-full">Users</span>
                <span class="bg-green-100 text-green-800 text-sm px-3 py-1 rounded-full">Messages</span>
                <span class="bg-green-100 text-green-800 text-sm px-3 py-1 rounded-full">Dates</span>
                <span class="bg-green-100 text-green-800 text-sm px-3 py-1 rounded-full">Media</span>
            </div>
        </div>

        <!-- Search Results (Placeholder) -->
        <div class="p-4">
            <h3 class="text-lg font-semibold text-gray-800 mb-3">Recent Searches</h3>
            <ul class="space-y-2">
                <li class="flex items-center justify-between text-gray-700">
                    <span><i class="fas fa-history mr-2 text-gray-500"></i>Meeting notes</span>
                    <i class="fas fa-times text-gray-400 cursor-pointer"></i>
                </li>
                <li class="flex items-center justify-between text-gray-700">
                    <span><i class="fas fa-history mr-2 text-gray-500"></i>Project deadline</span>
                    <i class="fas fa-times text-gray-400 cursor-pointer"></i>
                </li>
            </ul>
            <p class="text-gray-500 text-sm mt-4">No results found. Try a different search term.</p>
        </div>
    </div>
</body>
</html> 